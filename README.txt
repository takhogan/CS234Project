Directory description:

app/ -- the mobile application. Can be deployed with 
`cordova run android`, `cordova run ios`, or `cordova run browser`, depending on
platform.

ClassifierWeb.py -- the webserver that runs the web classifier

SoundClassifier.py -- the actual sound classifier

location/ -- code for the minuature location tracker that was to be used to collect
GPS data (as described in the project report)